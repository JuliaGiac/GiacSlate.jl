# Per-notebook metadata consumed by `resolve_notebook_meta` (DocumenterSlate §10).
#
# The *title* is deliberately absent: it is resolved from the notebook itself (a
# `title`-tagged cell → the first H1 → the filename). Only the fields KaimonSlate has no
# opinion on are declared here: `order`, `skip`, `show_code`, `binds`.

# The way in: algebra, calculus, linear algebra, interactive Taylor series.
[[notebook]]
path = "giac_intro.jl"
order = 1

# The full map of the Xcas engine, driven through live math fields.
[[notebook]]
path = "giac_tour.jl"
order = 2

# The physics lesson, with self-grading exercises.
[[notebook]]
path = "laplace_lesson.jl"
order = 3

# The widget playground. `show_code = true` is the default, restated here because on
# this page the code *is* the subject: what a reader comes to look at is the extension
# points, not the results.
[[notebook]]
path = "custom_controls.jl"
order = 4
show_code = true
